

const PushIntoLobby = async (boardPath: string, boardID:string) => {
    // const router = useRouter()

    // router.isReady ? router.push(boardPath + "/" + boardID) : null
}

export default PushIntoLobby