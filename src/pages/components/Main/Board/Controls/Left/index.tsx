import styles from "./index.module.scss"
import LeftIcon from "/Left.png"

const Left = () => {
    return <section className={styles["section"]}><div className={styles["container"]}>&lt;</div></section>
}

export default Left